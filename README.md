# Personal Website V2

## Development

Install dependencies:

```bash
npm install
```

Start the dev server:

```bash
npm run start
```

Build for production:

```bash
npm run build
```

On Windows PowerShell, `npm` may resolve to `npm.ps1` and fail if script execution is disabled. Use `npm.cmd` instead, or run the same commands from Command Prompt.
